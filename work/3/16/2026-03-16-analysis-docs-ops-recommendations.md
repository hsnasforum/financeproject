# 2026-03-16 analysis_docs 운영 정리 메모

## 이번 배치에서 다룬 문서
- 운영 정리 메모만 추가

## 무엇을 확인했고 무엇을 고쳤는지
- `git status --short -- analysis_docs work/3/16` 기준으로 `analysis_docs/**`가 현재 Git 추적 대상이 아니고, `work/3/16`에는 유사한 `analysis-docs-*` 메모가 여러 개 공존한다는 점을 다시 확인했습니다.
- 이번 라운드에서는 정책 변경, 파일 이동, 삭제, 병합은 수행하지 않았고 현재 상태와 추천안만 기록합니다.

## 아직 확정 못 한 항목
- `[보류]` `analysis_docs/**`를 장기 운영 문서로 유지할지, 일회성 산출물로 둘지는 저장소 운영 결정이 필요합니다.
- `[보류]` `work/3/16`의 유사 메모를 어떤 기준으로 대표본과 후속본으로 나눌지 팀 규칙이 없습니다.

## 추천안
- `analysis_docs/**`를 계속 검증 가능한 기준 문서로 쓸 계획이라면 Git 추적 대상으로 전환하는 편이 안전합니다. 지금 상태에서는 일반 `git diff --check`가 내용 변경보다 untracked 상태만 보여 주기 쉽습니다.
- `work/3/16`의 `analysis-docs-*` 메모는 이번 라운드처럼 삭제 없이 추가만 유지하되, 다음부터는 `문서번호-주제-행동` 형태의 slug를 고정해서 대표 기록과 후속 기록을 구분하는 편이 낫습니다.
- 중복 정리는 자동 수행하지 말고, 다음 운영 정리 라운드에서 `유지`, `참고`, `보관` 기준을 먼저 정한 뒤 일괄 적용하는 편이 안전합니다.

## 실행한 검증
- `git status --short -- analysis_docs work/3/16`

## 다음 우선순위
- `analysis_docs/**` Git 추적 여부를 결정한 뒤 문서 검증 절차를 다시 단순화할지 검토
- `/work` 메모 네이밍과 대표본 규칙을 별도 운영 규칙으로 정리할지 검토

## 사용 skill
- `work-log-closeout`: 운영 메모 형식과 남은 쟁점/추천안 구조를 저장소 관례에 맞추는 용도
