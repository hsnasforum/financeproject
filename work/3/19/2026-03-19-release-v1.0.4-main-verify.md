# 2026-03-19 v1.0.4 main 릴리즈 검증 정리

## 변경 파일
- `work/3/19/2026-03-19-n3-first-compliant-exemplar-capture.md`
- `tests/dart-search-client.test.tsx`
- `tests/planning-v3-news-settings-ui.test.tsx`
- `work/3/19/2026-03-19-release-v1.0.4-main-verify.md`
- 검증 대상 worktree는 위 변경 외에도 현재 dirty 상태 전체를 포함했고, final gate는 해당 전체 상태를 기준으로 실행했다.

## 사용 skill
- `planning-gate-selector`: current dirty worktree 기준으로 stable release-bound run의 final gate를 `pnpm release:verify -> pnpm build -> pnpm e2e:rc`로 고정하고 conditional/advisory 범위를 분리하는 데 사용했다.
- `work-log-closeout`: 실제 실행한 gate, advisory, evidence, 미실행 gate, exemplar 판정을 current release template 순서로 정리하는 데 사용했다.

## 대상 릴리즈
- version: `v1.0.4`
- branch: `pr37-planning-v3-txn-overrides`
- 기준 작업트리: `/home/xpdlqj/code/finance`
- release owner: Codex 단일 소유 final gate run
- closeout 유형: success형

## primary / companion final gate
- `pnpm release:verify`
  - 시도 1: FAIL
  - 첫 blocker: `multi-agent:guard`
  - 원인: latest `/work` note `work/3/19/2026-03-19-n3-first-compliant-exemplar-capture.md`에 `## 다음 라운드` 또는 `## 다음 작업` 헤더가 없어 guard가 중단됨
  - 조치: note 헤더를 `## 다음 라운드 우선순위`로 보정
  - 시도 2: FAIL
  - 첫 blocker: `multi-agent:guard`
  - 원인: 당시 latest `/work` note로 인식된 `work/3/19/2026-03-19-march-work-report-through-17.md`가 guard 요구 섹션을 갖추지 않음
  - 조치: current run용 release closeout note를 먼저 생성해 latest `/work` 기준을 현재 run으로 고정
  - 시도 3: FAIL
  - 첫 blocker: `pnpm test`
  - 원인: `tests/dart-search-client.test.tsx`, `tests/planning-v3-news-settings-ui.test.tsx`의 stale UI expectation
  - 조치: 현재 UI copy 기준으로 expectation 보정
  - 시도 4: PASS
- `pnpm build`
  - 결과: PASS
  - 비고: `node scripts/next_build_safe.mjs --webpack` 경로로 실행되어 isolated build dir `.next-build`를 사용
- `pnpm e2e:rc`
  - 결과: PASS
  - 비고: `13 passed (2.0m)`

## conditional minor guard
- `pnpm planning:v2:guard`
  - 실행됨 (`pnpm release:verify` 내부)
  - 결과: PASS
- `pnpm planning:v2:engine:guard`
  - 없음
  - 이유: 현재 run에서 engine envelope/report contract/fallback 직접 변경으로 판단하지 않음

## advisory record
- `pnpm planning:v2:freeze:guard`
  - 실행됨 (`pnpm release:verify` 내부)
  - 결과: WARN (informational, exit=0)
  - 요약: changed files 67, `src/app/planning/reports/page.tsx`를 v2 core change로 감지했고 `pnpm planning:v2:complete`, `pnpm planning:v2:regress` 필요성과 `[v2-core-change]` tag 미기재를 알림
- `pnpm planning:ssot:check`
  - 실행됨 (`pnpm release:verify` advisory stage)
  - 결과: PASS
- `pnpm planning:current-screens:guard`
  - 실행됨 (`pnpm release:verify` advisory chain)
  - 결과: PASS

## evidence 위치
- planning regression evidence: `.data/planning/eval/latest.json`
- scheduler/ops raw evidence
  - 없음
- support bundle / 추가 raw evidence
  - 없음
- gate 출력은 터미널 실행 로그 기준으로만 확인했고, raw output 전체는 본문에 복사하지 않음

## 미실행 gate
- 없음

## 검증
- `node -p "require('./package.json').version"`
  - 결과: `1.0.4`
- `git branch --show-current`
  - 결과: `pr37-planning-v3-txn-overrides`
- `git status --short`
  - 결과: current dirty worktree 확인
- `git diff --name-only --diff-filter=ACMRTUXB HEAD`
  - 결과: tracked dirty 파일 목록 확인
- `git ls-files --others --exclude-standard`
  - 결과: untracked dirty 파일 목록 확인
- `pnpm release:verify`
  - 결과: FAIL (`gate=multi-agent:guard`)
- `pnpm release:verify`
  - 결과: FAIL (`gate=multi-agent:guard`)
- `pnpm release:verify`
  - 결과: FAIL (`gate=pnpm test`)
- `pnpm test tests/dart-search-client.test.tsx tests/planning-v3-news-settings-ui.test.tsx`
  - 결과: PASS (`2 files, 4 tests`)
- `pnpm release:verify`
  - 결과: PASS
- `pnpm build`
  - 결과: PASS
- `pnpm e2e:rc`
  - 결과: PASS (`13 passed`)
- `git diff --check -- work/3/19/2026-03-19-n3-first-compliant-exemplar-capture.md tests/dart-search-client.test.tsx tests/planning-v3-news-settings-ui.test.tsx work/3/19/2026-03-19-release-v1.0.4-main-verify.md`
  - 결과: PASS

## 남은 리스크
- `pnpm planning:v2:freeze:guard`가 `src/app/planning/reports/page.tsx`를 v2 core change로 계속 감지하고 `[v2-core-change]` tag 미기재를 advisory로 남긴다.
- 현재 release closeout note는 이번 run 기준으로는 current template 완전 준수이므로 `first compliant exemplar candidate`로 기록할 수 있다. 다만 실제 exemplar 채택 여부는 후속 release commit/merge 이후 별도 판단이 필요하다.
- next owner / 다음 단계
  - 현재 release closeout note와 이번 라운드의 테스트 expectation 보정 두 건을 함께 commit 범위에 포함해 latest `/work` note tracking 상태를 안정화한다.
  - 후속 exemplar review에서는 이 note를 legacy success/blocker exemplar와 별도인 first compliant candidate로만 비교한다.

## 다음 작업
- actual release commit 또는 handoff 시 이 closeout note를 기준으로 evidence 경로와 advisory를 다시 복사하지 말고 참조만 유지한다.
